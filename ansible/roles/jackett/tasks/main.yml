- name: Install Docker
  apt:
    name:
      - docker.io
      - docker-compose
      - python3-docker
    state: present
    update_cache: yes

- name: Create directories
  file:
    path: /opt/jackett
    state: directory
    mode: '0755'

- name: Copy docker-compose
  copy:
    src: docker-compose.yml
    dest: /opt/jackett/docker-compose.yml

- name: Run docker-compose
  command: docker-compose -f /opt/jackett/docker-compose.yml up -d
